<%= form_for(@private_league) do |f| %>
  <% if @private_league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@private_league.errors.count, "error") %> prohibited this private_league from being saved:</h2>

      <ul>
      <% @private_league.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :league_id %><br />
    <%= f.number_field :league_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
